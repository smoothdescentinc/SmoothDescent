import React from 'react';
import { HashRouter } from 'react-router-dom';
import App from './App';

export default function Main() {
    return (
        <HashRouter>
            <App />
        </HashRouter>
    );
}
